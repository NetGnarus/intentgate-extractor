"""IntentGate intent extractor service.

Turns free-form user prompts into structured intent (summary,
allowed_tools, forbidden_tools) the gateway can use to enforce the
second of its four checks.
"""

__version__ = "0.1.0"
